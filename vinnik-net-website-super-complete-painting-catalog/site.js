document.addEventListener('DOMContentLoaded',()=>{const nav=document.querySelector('[data-nav]'),menu=document.querySelector('[data-nav-toggle]');menu?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',String(open));menu.textContent=open?'Close':'Menu'});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));const theme=document.querySelector('[data-theme-toggle]'),themeKey='vinnik-theme';if(localStorage.getItem(themeKey)==='dark')document.body.classList.add('theme-dark');theme?.addEventListener('click',()=>{document.body.classList.toggle('theme-dark');localStorage.setItem(themeKey,document.body.classList.contains('theme-dark')?'dark':'light')});const scopeKey='vinnik-scope';const getScope=()=>JSON.parse(localStorage.getItem(scopeKey)||'[]'),saveScope=x=>localStorage.setItem(scopeKey,JSON.stringify([...new Set(x)]));const updateScope=()=>{document.querySelectorAll('[data-scope-count]').forEach(x=>x.textContent=getScope().length);const field=document.querySelector('[data-scope-field]');if(field)field.value=getScope().join('\n')};document.querySelectorAll('[data-scope]').forEach(btn=>btn.addEventListener('click',()=>{const s=getScope();s.push(btn.dataset.scope);saveScope(s);updateScope();notify(btn.dataset.scope+' added to project scope')}));document.querySelector('[data-scope-clear]')?.addEventListener('click',()=>{saveScope([]);updateScope()});updateScope();const search=document.querySelector('[data-catalog-search]'),filter=document.querySelector('[data-catalog-filter]'),cards=[...document.querySelectorAll('[data-catalog] .searchable')],empty=document.querySelector('[data-empty]');function apply(){if(!cards.length)return;const q=(search?.value||'').toLowerCase().trim(),cat=filter?.value||'all';let n=0;cards.forEach(c=>{const show=(!q||(c.dataset.search||'').includes(q))&&(cat==='all'||c.dataset.category===cat);c.hidden=!show;if(show)n++});if(empty)empty.hidden=n>0}search?.addEventListener('input',apply);filter?.addEventListener('change',apply);const toast=document.querySelector('[data-toast]');let tt;function notify(m){if(!toast)return;toast.textContent=m;toast.classList.add('show');clearTimeout(tt);tt=setTimeout(()=>toast.classList.remove('show'),2400)}document.querySelectorAll('[data-demo-form]').forEach(f=>f.addEventListener('submit',e=>{e.preventDefault();const s=f.querySelector('.form-status');if(s)s.textContent='Demo only — no information was transmitted. Connect a real CRM/form endpoint before launch.';notify('Demo request prepared — nothing was sent.')}));document.querySelectorAll('main > section').forEach(x=>x.classList.add('reveal'));if('IntersectionObserver'in window){const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.06});document.querySelectorAll('.reveal').forEach(x=>io.observe(x))}else document.querySelectorAll('.reveal').forEach(x=>x.classList.add('visible'))})